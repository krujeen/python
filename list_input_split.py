# อ่านข้อมูลหลาย ๆ ตัวที่ผู้ใช้ป้อนเข้ามาในบรรทัดเดียวกัน โดยข้อมูลแต่ละตัวคั่นด้วยช่องว่าง แล้วเก็บไว้ใน list
a  = []
a.append(input().split())


# อ่านข้อมูลหลาย ๆ ตัวที่ผู้ใช้ป้อนเข้ามาในบรรทัดเดียวกัน โดยข้อมูลแต่ละตัวคั่นด้วยช่องว่าง แล้วเก็บไว้ใน list
b = []
b = [e for e in input().split()] 

# อ่านข้อมูลหลาย ๆ ตัวที่ผู้ใช้ป้อนเข้ามาในบรรทัดเดียวกัน โดยข้อมูลแต่ละตัวคั่นด้วยช่องว่าง  แปลงเป็น integer แล้วเก็บไว้ใน list 
c = []
c = [int(e) for e in input().split()] 

